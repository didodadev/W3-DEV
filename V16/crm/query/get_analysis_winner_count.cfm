<cfquery name="GET_ANALYSIS_WINNER_COUNT" datasource="#dsn#">
	SELECT
		COUNT(RESULT_ID) AS TOTAL_WINNERS
	FROM
		MEMBER_ANALYSIS_RESULTS
	WHERE
		ANALYSIS_ID = #attributes.ANALYSIS_ID#
	AND
		USER_POINT >= AVERAGE
</cfquery>
