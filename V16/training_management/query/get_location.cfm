<cfquery name="GET_LOCATION" datasource="#dsn#">
	SELECT 
		DEPARTMENT.DEPARTMENT_HEAD, 
		BRANCH.BRANCH_NAME,
		ZONE.ZONE_NAME,
		BRANCH.COMPANY_ID
	FROM 
		BRANCH,
		DEPARTMENT,
		ZONE,
		EMPLOYEE_POSITIONS
	WHERE
		ZONE.ZONE_ID = BRANCH.ZONE_ID
		AND
		BRANCH.BRANCH_ID=DEPARTMENT.BRANCH_ID
		AND
		<!---DEPARTMENT.DEPARTMENT_ID = #attributes.DEPARTMENT_ID#--->
		DEPARTMENT.DEPARTMENT_ID = EMPLOYEE_POSITIONS.DEPARTMENT_ID
		AND
		EMPLOYEE_POSITIONS.EMPLOYEE_ID = #attributes.EMP_ID#
</cfquery>
